[build]
builder = "nixpacks"

[deploy]
startCommand = "gunicorn demo.app:app --workers 4 --threads 2 --timeout 120 --bind 0.0.0.0:$PORT"
healthcheckPath = "/"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
